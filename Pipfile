[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
django = "~=5.2.0"
djangorestframework = ">=3.14.0"
django-filter = ">=23.5"
pillow = ">=10.0.0"
openpyxl = ">=3.1.2"
reportlab = ">=4.0.0"
qrcode = ">=7.4.2"
python-dotenv = ">=1.0.0"
requests = ">=2.31.0"
mysqlclient = ">=2.2.0"

[dev-packages]
pytest = ">=7.4.0"
pytest-django = ">=4.5.0"
flake8 = ">=6.0.0"

[requires]
python_version = "3.10"
